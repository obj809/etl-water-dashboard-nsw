# .env.docker - Example environment file for Docker

# API credentials (required)
API_KEY=your_waterinsights_api_key
API_SECRET=your_waterinsights_api_secret

# Database connection
# DB_HOST is overridden to 'host.docker.internal' in docker-compose.yml
# to connect to MySQL running on your local machine
DB_HOST=localhost
DB_PORT=3306
DB_USER=water_user
DB_PASSWORD=water_password
DB_NAME=water_dashboard
