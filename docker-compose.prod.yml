# docker-compose.prod.yml
# Production deployment - connects to external database

services:
  etl:
    build: .
    container_name: water-etl
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
      - ./data:/app/data
    restart: "no"
